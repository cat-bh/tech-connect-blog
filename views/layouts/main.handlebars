<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Connect</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
        integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
</head>

<body>
    <div class="container-fluid p-0">
        <header class="row d-flex justify-content-between align-items-end w-100 p-4 bg-info m-0">
            <h1 class="col text-warning m-0">
                Tech Connect
            </h1>
            <nav class="col d-flex justify-content-between align-items-center">
                <a href="/" class="text-light">Homepage</a>
                <a href="/dashboard" class="text-light">Dashboard</a>
                {{#if loggedIn}}
                <button class="btn btn-outline-light" id="logout">Log Out</button>
                {{else}}
                <a href="/login">
                    <button class="btn btn-outline-light">Login</button>
                </a>
                {{/if}}

            </nav>
        </header>

        <main class="m-5">
            {{{ body }}}
        </main>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>